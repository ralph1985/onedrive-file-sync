# Microsoft Graph app registration (personal OneDrive uses consumers tenant)
CLIENT_ID=
TENANT_ID=consumers

# Redirect URI configured for your app (e.g. http://localhost:53682)
REDIRECT_URI=

# Optional: store tokens if you choose to manage auth manually
# ACCESS_TOKEN=
# REFRESH_TOKEN=

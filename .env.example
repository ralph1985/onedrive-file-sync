# Local absolute path to the file you want to sync
LOCAL_FILE_PATH=

# OneDrive path (relative to root), e.g. Documents/mi.txt
ONEDRIVE_FILE_PATH=

# Microsoft Graph app registration (personal OneDrive uses common tenant)
CLIENT_ID=
TENANT_ID=common

# Redirect URI configured for your app (e.g. http://localhost:53682)
REDIRECT_URI=

# Optional: store tokens if you choose to manage auth manually
# ACCESS_TOKEN=
# REFRESH_TOKEN=
